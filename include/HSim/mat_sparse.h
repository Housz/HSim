#pragma once

#include <iostream>

namespace HSim
{

    /**
     * @brief Sparse Matrix using Compressed Sparse Row(CSR) Representation.
     * 
     * @tparam T : float or double
     * @tparam M : num of rows
     * @tparam N : num of cols
     */
    template <typename T, size_t M, size_t N>
    class MatCSR
    {
    public:

    

    };

} // namespace HSim
